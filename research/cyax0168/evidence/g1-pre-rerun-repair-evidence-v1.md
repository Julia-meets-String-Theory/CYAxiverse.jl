# CYAX-0168 G1 pre-rerun repair evidence v1

Status: **repair scaffolding only; no G1 rerun or gate claim**

This packet records implementation checks for the bounded R1/R3/R5/R6 repair
at starting head `cd5112ed6afb41937c42ffa9354b5ecc554f4d31`. It does not enter
G1, access T0–T4 decision fixtures, or change the approved specification.

## R1 — one current-run host context

`g1-pre-rerun-host-manifest-v2.json` is the sole current-run manifest for a
future packet. It was regenerated by one fresh `capture_host_manifest()` call
in this worktree context; its immutable SHA-256 identity is
`b44f7f33c5da253ffba913a072c7a5e8ef173696fdd6ef39d192015075afc7ce` and its
fresh `shutil.disk_usage` values are frozen in the artifact.
The `cyax-capacity-terms-v1` mapping explicitly binds
`total_volume_capacity` to the post-operation reserve denominator and
`preflight_available_volume_capacity` to the ordinary available-capacity API
and disk hard/allowance denominator. Loader hash, capacity-mapping, tamper,
and mixed-context tests pass.

## R3 — exact pre-rerun identity packet

`g1-pre-rerun-execution-packet-v1.json` binds candidate head
`4ed8293fd5483f98eba6f4603c5b8d34e945f4bd` to the host-manifest identity
above. The packet context hash is
`4c9e36b467f1817b48b773c2dd26c9864c6042f9b10572da411277a42fd54a94` and the
packet hash is
`f1809d142acde02ba8d31e38c2b1c43fea851cac2035392cbf04fb1f20796dd9`.
Validation rejects a different host or a changed packet field.

## R5 — lossless candidate-vector trace

Both generator implementations store each frozen PRF candidate vector once
under a content-addressed `candidate_vector_id`; each choice records the ID,
count, attempts, rejection causes, and selected value. Reconstruction restores
the exact ordered vector on demand. The focused C0 conformance comparison
passes all checks, with 3,566 PRF choices represented by 97 deduplicated
vectors and unchanged logical checksum
`e73063a46e1c585bfb15aa0db5e920a0f6b4a37f2b5a44a7a0b372f7aecd3b18`.

## R6 — direct thermal-state control

The macOS route is the Foundation `NSProcessInfo.processInfo.thermalState`
property through the Objective-C runtime (`libobjc.A.dylib`, mechanism
`macos-foundation-nsprocessinfo-thermal-v1`). Enum values 0/1/2/3 map exactly
to nominal/fair/serious/critical. Each pre/during/post phase performs one
synchronous property read; the fixed nominal-only gate invalidates unknown,
fair, serious, critical, unavailable, out-of-order, over-gap, or monitor-gap
observations. The notification name
`NSProcessInfoThermalStateDidChange` (not subscribed; polling is authoritative),
one-second inter-phase interval, five-second maximum gap, and two-second sample
bound are recorded in each
observation's frozen timing identity. A local direct preflight returned
`nominal` for all three declared phases with `available=true`; exact
observations and timing are recorded in
`g1-pre-rerun-thermal-preflight-v1.json`. Injected tests cover all four enum
values and unavailable output, while the live result was accepted only from
the Foundation property read.

## Checks

- `python3 -m unittest research.cyax0168.test_host research.cyax0168.test_execution_packet research.cyax0168.test_measurement -v` — 14/14 passed.
- `python3 scripts/agent_verify.py run -- python3 -m unittest discover -s research/cyax0168 -p 'test*.py'` — passed.
- `python3 scripts/agent_verify.py run -- python3 -m unittest discover -s research/cyax0168/tests -p 'test*.py'` — passed.
- `PYTHONPATH=. python3 -m research.cyax0168.compare_generator_23 --tier C0 --profile P-medium --seed 168900` — passed; the CLI now registers all 16 approved C0–C3 cells and C0 compared complete canonical serialized bytes plus complete generator manifests; no T0–T4 access.
- `python3 scripts/agent_verify.py diff-check` and `git diff --check` — passed.
- Direct local Foundation preflight — completed; all three observations were
  available and nominal through `NSProcessInfo.processInfo.thermalState`.

Generated cache directories were removed after verification. Any later
material change to the candidate repair or frozen host manifest invalidates
this packet and requires a new exact-state review.
