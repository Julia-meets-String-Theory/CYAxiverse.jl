# CYAX-0168 tasks

These tasks decompose evidence readiness. GitHub remains authoritative for live
Issue, Project, review, PR, and merge state.

- [ ] T1 — Obtain independent architecture/methodology review and owner
  approval of the exact design head.
  - Outcome: approved backend pin, workload, resource envelopes, repetitions,
    and decision thresholds.
  - Verify: durable approval references the exact revision; otherwise stop.
- [ ] T2 — Freeze and validate the #117 canonical source bundle and K1–K12
  expected assertions.
  - Outcome: citation-bearing real semantic fixture at one observation boundary.
  - Verify: source hashes and independent known-answer review.
- [ ] T3 — Implement the common assertion snapshot and semantic evaluator.
  - Outcome: canonical rebuildable snapshot plus one authority-safe evaluator.
  - Verify: schema, checksum, tamper, stale-view, admissibility, temporal,
    supersession, dispute, and staleness tests.
- [ ] T4 — Implement and freeze the inert scale generator.
  - Outcome: exact T0–T4 fixtures for the declared seeds/topology profiles.
  - Verify: exact counts, invariants, repeated byte/checksum equality, and no
    scientific claims.
- [ ] T5 — Implement the indexed SQLite S baseline.
  - Outcome: normalized disposable view and Q01–Q12 adapter.
  - Verify: DDL/index/query-plan review, deterministic semantic export, and no
    JSONL scan control.
- [ ] T6 — Repeat the primary candidate gate and implement G.
  - Outcome: pinned offline Ladybug view and Q01–Q12 adapter, or a stopped and
    reviewed fallback amendment.
  - Verify: release/hash/license/import/offline/build/rebuild/reopen/path-query
    evidence and deterministic semantic export.
- [ ] T7 — Freeze the workload and shared output/context pipeline.
  - Outcome: ID-addressed Q01–Q12 inputs, golden results, canonical
    `RetrievalBundle`, and one context compiler/tokenizer.
  - Verify: stable ordering, exact semantic sets, bundle validation, and
    diagnostics-stripped S/G byte equality.
- [ ] T8 — Implement and calibrate the measurement harness.
  - Outcome: randomized paired storage/build/update/RSS/latency/context metrics.
  - Verify: synthetic raw records, deterministic aggregates, percentile and
    confidence-interval tests, and environment manifest.
- [ ] T9 — Under a later dispatch, run G1/G2 and T0–T3.
  - Outcome: correctness-admissible measurements or a preserved architecture
    failure.
  - Verify: every fixture passes semantic parity before timing is admitted;
    stop on mismatch or resource breach.
- [ ] T10 — Conditionally run T4, classify, independently review, and report.
  - Outcome: S wins, G wins, Hybrid wins, or Architecture problem, with raw and
    aggregate evidence and limitations.
  - Verify: T3 eligibility, approved T4 envelope, deterministic classifier,
    independent evidence review, and #168/#162 handoff.

Do not start T2–T10 from this design commit alone. The next action is review of
the draft packet and explicit owner decisions, not benchmark execution.
