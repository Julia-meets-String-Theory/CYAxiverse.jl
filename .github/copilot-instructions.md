# GitHub Copilot adapter

Follow the canonical repository instructions in `AGENTS.md`.

For implementation work, inspect the affected code and tests, keep changes
scoped, preserve unrelated working-tree changes, and use the repository's
existing verification paths (including `scripts/agent_verify.py`) rather than
generic boilerplate. Project-specific scientific or numerical work may require
one of the tracked skills under `.agents/skills/`.
