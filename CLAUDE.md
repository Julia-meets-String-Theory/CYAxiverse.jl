# Claude Code adapter

Read and follow `AGENTS.md`; it is the canonical CYAxiverse.jl agent contract.
Load only the project skill(s) relevant to the current task. Claude-specific
plugins, permissions, and response-style preferences are local user settings
and are not repository policy.
